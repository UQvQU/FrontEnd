# this

- 参数
  js的参数较为松散，有arguments数组保存参数
  event 在第一位置 事件注册与事件处理函数分离
  event.target event.target.tagName
  this 一般指向事件发生的元素，但箭头函数内部的this丢失(默认指向上一级)

- ajax fetch 让前端可以主动发起请求 --> 动态网页
  fetch函数 返回可读流
